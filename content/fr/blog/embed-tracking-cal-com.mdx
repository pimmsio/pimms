---
title: Suis tes liens Cal.com intégrés avec l’IA ou des outils no-code
summary: Apprends à suivre les leads provenant de ton calendrier Cal.com grâce à des outils intelligents — sans configuration technique requise.
publishedAt: 2025-04-30
updatedAt: 2025-04-30
image: https://assets.pimms.io/pimms-calcom-integration-zapier.webp?v=2
author: alexandre
categories:
  - education
related:
  - start-with-cal-com-and-zapier
  - setup-stripe-for-website
  - introducing-conversion
---

Tu utilises Cal.com pour prendre des rendez-vous en ligne ?

Avec PiMMs, tu peux enfin savoir d'où viennent les visiteurs qui prennent rendez-vous — sans configuration technique compliquée.

Voici comment activer le suivi selon la méthode d’intégration Cal.com que tu utilises :

---

## Script de base à ajouter

Ajoute ce script sur toutes les pages où tu veux activer le suivi :

```html
<script defer src="https://cdn.pimms.io/analytics/script.detection.js"></script>
```

---

## Cas 1 : Calendrier intégré (embed Cal.com avec `data-cal-link`)

Si tu as intégré Cal.com sous forme de bouton (embed), ajoute simplement `?pimms_id=1` à la fin de ton lien. Exemple :

```html
<button data-cal-link="alexandre/30min?pimms_id=1">Réserver</button>
```

C’est tout. PiMMs remplacera automatiquement `1` par l’identifiant du visiteur.



---

## Cas 2 : Calendrier inline (Cal.com intégré dans la page avec JavaScript)

Si tu as intégré Cal directement dans la page (inline), suis ces deux étapes :

1. Ajoute ce script au-dessus du script Cal.com :

```html
<script src="https://cdn.pimms.io/analytics/script.expose.js"></script>
```

2. Ajoute `?pimms_id=${window.pimms_id}` dans le lien Cal :

```js
Cal.ns["30min"]("inline", {
  elementOrSelector: "...",
  calLink: `alexandre/30min?pimms_id=${window.pimms_id}`
});
```

Et voilà. PiMMs détectera chaque visiteur automatiquement.

---

## Cas 3 : Lien simple (non embed)

Si tu utilises un lien Cal.com classique dans ta page :

```html
<a href="https://cal.com/alexandre/30min?pimms_id=1">Réserver</a>
```

Ajoute `?pimms_id=1` à la fin. PiMMs le mettra à jour au bon moment.

---

Ton calendrier est maintenant traçable automatiquement.

Fonctionne avec tous les outils no-code.

Copie, colle, c’est suivi 🚀